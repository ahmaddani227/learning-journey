# list -> array, mengakses dengan menggunakan index

data_list = ['upin', 'ipin', 'adit', 'sopo', 'jarwo']

print(data_list[0])

# dictonary (dict) -> associative array
# identifier -> key

data_dict = {
    'key': 'value',
    'ad': 'adit',
    'sp': 'sopo',
    'jrw': 'jarwo',
    'eps': 100,
    'list': data_list
}

print(data_dict['ad'])
print(data_dict['eps'])
print(data_dict['list'])