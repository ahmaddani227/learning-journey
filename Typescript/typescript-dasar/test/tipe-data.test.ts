describe("Data Type", () => {
  it("should must declare", () => {
    const name: string = "Ahmad Dani";
    const year: number = 2024;
    const isVip: boolean = true;

    console.info(name);
    console.info(year);
    console.info(isVip);
  });
});
